# 🎓 NATK Schedule Parser

Парсер расписания занятий Новосибирского авиационного технического колледжа. Проект предоставляет API для получения расписания групп и информации о занятиях.

## 🚀 Возможности

- 📅 Получение списка всех учебных групп с номерами курсов
- 🕐 Парсинг детального расписания занятий для каждой группы
- 👨‍🏫 Информация о преподавателях и аудиториях
- 🐳 Docker контейнеризация для простого развертывания
- 🔄 Автоматическое обновление через GitHub Actions

## 🛠 Технологии

- **.NET 10.0** - основной фреймворк
- **HTML Agility Pack** - парсинг HTML
- **Docker** - контейнеризация
- **GitHub Actions** - CI/CD
- **Portainer** - управление контейнерами на VPS

## 📦 Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/natk-parser.git
cd natk-parser
```

2. Восстановите зависимости:
```bash
dotnet restore
```

3. Запустите приложение:
```bash
dotnet run
```

### Docker запуск

```bash
# Сборка образа
docker build -t natk-parser .

# Запуск контейнера
docker run -d -p 8080:80 --name natk-parser natk-parser
```

## 🎯 Использование

### Docker Image

Клонирование образа

```
docker pull xomamdk/natk-parser
```

## 🔧 API Endpoints

Приложение предоставляет следующие HTTP endpoints:

- `GET /api/groups` - список всех групп
- `GET /api/schedule/{groupName}` - расписание группы

## 🏗 Архитектура

```
NatkParser/
├── Models/           # Модели данных
│   ├── GroupInfo.cs
│   ├── Lesson.cs
│   └── Schedule.cs
├── Services/         # Бизнес-логика
│   └── ParserService.cs
├── Controllers/      # Web API контроллеры
└── Program.cs        # Точка входа
```

## 🔄 CI/CD Pipeline

Проект использует GitHub Actions для автоматической сборки и развертывания:

1. **Build & Test** - сборка и тестирование приложения
2. **Docker Build & Push** - сборка Docker образа и публикация в Docker Hub
3. **Deploy** - автоматическое развертывание на VPS через SSH

## 🐛 Отладка и логи

Приложение логирует ключевые события:
- Успешный парсинг групп и расписаний
- Ошибки сетевых запросов
- Проблемы с парсингом HTML

Логи доступны через стандартный вывод Docker контейнера.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE.txt).

## 🤝 Контрибьютинг

1. Форкните репозиторий
2. Создайте feature ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add some amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

<div align="center">

**Разработано с ❤️ для студентов НАТК**

</div>
